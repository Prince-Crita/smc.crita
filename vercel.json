{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "//regions": "sin1 = Singapore, the SAME region as the Neon database (ap-southeast-1), and the closest Vercel region to the users in India. Every API route makes several sequential database round-trips; running the functions in iad1 (Washington DC) put ~15,000 km between the function and the database, so each of those round-trips cost ~230ms of pure distance and the simplest endpoint could not respond in under half a second. Keep this in the database's region — if the database is ever moved, move this with it.",
  "regions": ["sin1"],
  "env": {
    "NODE_ENV": "production"
  }
}
